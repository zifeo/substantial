# demo:
#   watch: [ "demo/**/*.py", "substantial/**/*.py" ]
#   command: |
#     poetry shell
#     python3 demo/main.py
#   depends_on: setup

setup:
  workdir: "."
  command: |
    source .venv/bin/activate
    poetry install

sync-test:
  workdir: "."
  command: |
    source .venv/bin/activate
    python3 tests/sync/test.py

async-test:
  workdir: "."
  command: |
    source .venv/bin/activate
    pytest tests/async/test.py
